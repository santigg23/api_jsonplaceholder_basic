import { FilterByCityPipe } from './filter-by-city-pipe';

describe('FilterByCityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCityPipe();
    expect(pipe).toBeTruthy();
  });
});
